name: CI

on:
  # Only run push on main
  push:
    branches:
      - main
    paths-ignore:
      - '**/*.md'
  # Always run on PRs
  pull_request:

concurrency:
  group: 'ci-${{ github.event.merge_group.head_ref || github.head_ref }}-${{ github.workflow }}'
  cancel-in-progress: ${{ github.ref != 'refs/heads/main' }}

env:
  # By default the devkit resolves its version set from the `devkit.*` range properties in
  # gradle.properties — that full set is what `generate-matrix` reads. Steps that target one
  # specific version park those ranges outside any version that exists, so `testAgainst` alone
  # decides what gets configured. Expand unquoted, and always pair it with a `testAgainst` (an
  # empty one means "just the build's own Kotlin"). The build's own Kotlin always has a target, so
  # this narrows to one version, or two when the requested one differs from it.
  DEVKIT_NARROW: >-
    -Porg.jetbrains.kotlin.compiler.plugin.devkit.minCliVersion=99.0.0
    -Porg.jetbrains.kotlin.compiler.plugin.devkit.maxCliVersion=99.9.9
    -Porg.jetbrains.kotlin.compiler.plugin.devkit.minIdeaVersion=9999
    -Porg.jetbrains.kotlin.compiler.plugin.devkit.useLatestDev=false

jobs:
  format:
    name: format
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7

      - parallel:
          - name: Configure JDK
            uses: actions/setup-java@v5
            with:
              distribution: 'zulu'
              java-version-file: .github/workflows/.java-version

          - name: Cache kempt formatter binaries
            uses: actions/cache@v6
            with:
              path: ~/.kempt/cache
              key: kempt-${{ hashFiles('.kempt.toml') }}

      - name: Install kempt
        run: |
          curl --proto '=https' --tlsv1.2 -LsSf \
            https://github.com/ZacSweers/kempt/releases/latest/download/kempt-fmt-installer.sh | sh

      - name: Pre-warm kempt cache
        run: kempt update

      - name: Check formatting
        run: kempt check

  generate-matrix:
    name: "⊹ generate compiler matrix ⊹"
    runs-on: ubuntu-latest
    outputs:
      matrix: ${{ steps.generate.outputs.matrix }}
      default_kotlin_version: ${{ steps.generate.outputs.default_kotlin_version }}
      latest_kotlin_version: ${{ steps.generate.outputs.latest_kotlin_version }}
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7

      # The matrix comes from the devkit via `:compiler-compat:compilerVersions`, so this job needs a
      # configurable build. Read-only caches: no other job depends on what it would write.
      - name: Setup Gradle
        uses: ./.github/actions/setup-gradle
        with:
          encryption-key: ${{ secrets.GRADLE_ENCRYPTION_KEY }}
          cache-read-only: true

      - name: Generate matrix
        id: generate
        run: ./scripts/generate-ci-matrix.sh

  core:
    name: core artifacts
    runs-on: ubuntu-latest
    timeout-minutes: 40
    permissions:
      checks: write

    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7

      - name: Setup Gradle
        uses: ./.github/actions/setup-gradle
        with:
          encryption-key: ${{ secrets.GRADLE_ENCRYPTION_KEY }}
          # Keep one Linux Gradle cache writer on main.
          cache-read-only: ${{ github.ref != 'refs/heads/main' }}
          konan: true
          konan-cache-read-only: ${{ github.ref != 'refs/heads/main' }}

      - name: Check root project
        run: |
          ./scripts/run-ci-gradle.sh check --continue --quiet \
            -x :compiler:check \
            -x :compiler-tests:check \
            -x :gradle-plugin:check \
            $DEVKIT_NARROW '-Porg.jetbrains.kotlin.compiler.plugin.devkit.testAgainst=' \
            ${{ github.ref != 'refs/heads/main' && '-Dorg.gradle.configuration-cache.read-only=true' || '' }}

      - name: (Fail-only) Upload reports
        if: failure()
        uses: actions/upload-artifact@v7
        with:
          name: reports-core
          path: |
            **/build/reports/**

      - name: Publish Test Report
        uses: mikepenz/action-junit-report@v6
        if: failure()
        with:
          report_paths: '**/build/test-results/**/TEST-*.xml'
          check_name: 'Test Report - core'

  compatibility:
    name: "compiler compatibility / ${{ matrix.kotlin-compiler }}"
    runs-on: ubuntu-latest
    needs: generate-matrix
    timeout-minutes: 40
    strategy:
      matrix: ${{ fromJson(needs.generate-matrix.outputs.matrix) }}
      fail-fast: false
    env:
      METRO_TEST_OMIT_REDUNDANT_MIRRORS: ${{ matrix.kotlin-compiler == needs.generate-matrix.outputs.default_kotlin_version || matrix.kotlin-compiler == needs.generate-matrix.outputs.latest_kotlin_version }}
    permissions:
      checks: write

    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7

      - name: Setup Gradle
        uses: ./.github/actions/setup-gradle
        with:
          encryption-key: ${{ secrets.GRADLE_ENCRYPTION_KEY }}
          cache-read-only: true
          # NativeICTests runs independently of the JVM-only parameterized IC tests. Preserve its
          # per-version toolchain coverage while avoiding a second writer for the default version.
          konan: true
          konan-cache-read-only: ${{ github.ref != 'refs/heads/main' || matrix.kotlin-compiler == needs.generate-matrix.outputs.default_kotlin_version }}
          kotlin-compiler-version: ${{ matrix.kotlin-compiler }}

      - name: Generate compiler tests
        id: generate_tests
        run: |
          ./scripts/run-ci-gradle.sh :compiler-tests:generateTests --quiet \
            '-Porg.jetbrains.kotlin.compiler.plugin.devkit.defaultTestVersion=${{ matrix.kotlin-compiler }}' \
            $DEVKIT_NARROW \
            '-Porg.jetbrains.kotlin.compiler.plugin.devkit.testAgainst=${{ matrix.kotlin-compiler }}' \
            -Pmetro.excludeJsBoxTests \
            ${{ github.ref != 'refs/heads/main' && '-Dorg.gradle.configuration-cache.read-only=true' || '' }}

      - name: Test compiler
        run: |
          ./scripts/run-ci-gradle.sh :compiler:defaultTest :compiler-tests:defaultTest --continue --quiet \
            '-Porg.jetbrains.kotlin.compiler.plugin.devkit.defaultTestVersion=${{ matrix.kotlin-compiler }}' \
            $DEVKIT_NARROW \
            '-Porg.jetbrains.kotlin.compiler.plugin.devkit.testAgainst=${{ matrix.kotlin-compiler }}' \
            "-Pmetro.testOmitRedundantMirrors=$METRO_TEST_OMIT_REDUNDANT_MIRRORS" \
            -Pmetro.excludeJsBoxTests \
            ${{ github.ref != 'refs/heads/main' && '-Dorg.gradle.configuration-cache.read-only=true' || '' }}

      - name: Test Gradle plugin
        if: ${{ !cancelled() && steps.generate_tests.outcome == 'success' }}
        run: |
          ./scripts/run-ci-gradle.sh :gradle-plugin:functionalTest --continue --quiet \
            '-Porg.jetbrains.kotlin.compiler.plugin.devkit.defaultTestVersion=${{ matrix.kotlin-compiler }}' \
            $DEVKIT_NARROW \
            '-Porg.jetbrains.kotlin.compiler.plugin.devkit.testAgainst=${{ matrix.kotlin-compiler }}' \
            "-Pmetro.testOmitRedundantMirrors=$METRO_TEST_OMIT_REDUNDANT_MIRRORS" \
            -Pmetro.functionalTestKmpTarget=jvm \
            ${{ github.ref != 'refs/heads/main' && '-Dorg.gradle.configuration-cache.read-only=true' || '' }}

      - name: (Fail-only) Upload reports
        if: failure()
        uses: actions/upload-artifact@v7
        with:
          name: reports-compatibility-${{ matrix.kotlin-compiler }}
          path: |
            **/build/reports/**

      - name: Publish Test Report
        uses: mikepenz/action-junit-report@v6
        if: failure()
        with:
          report_paths: '**/build/test-results/**/TEST-*.xml'
          check_name: 'Test Report - compatibility / ${{ matrix.kotlin-compiler }}'

  shaded-compiler-smoke:
    name: shaded compiler / JDK 21
    runs-on: ubuntu-latest
    timeout-minutes: 40
    permissions:
      checks: write

    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7

      # Keep the build and R8 on the project's JDK 25 while making JDK 21 available for the
      # compiler-test and TestKit workers that execute the shaded compiler.
      - name: Configure test JDK
        uses: actions/setup-java@v5
        with:
          distribution: 'zulu'
          java-version: '21'

      - name: Capture test JDK home
        run: echo "METRO_TEST_JAVA_HOME=$JAVA_HOME" >> "$GITHUB_ENV"

      - name: Setup Gradle
        uses: ./.github/actions/setup-gradle
        with:
          encryption-key: ${{ secrets.GRADLE_ENCRYPTION_KEY }}
          cache-read-only: true

      - name: Test shaded compiler
        run: |
          ./scripts/run-ci-gradle.sh :compiler-tests:defaultTest --quiet \
            $DEVKIT_NARROW \
            -Pmetro.testJavaVersion=21 \
            "-Dorg.gradle.java.installations.paths=$METRO_TEST_JAVA_HOME" \
            --tests 'dev.zacsweers.metro.compiler.ShadedCompilerTest.optional terminal providers initialize from the shaded compiler jar' \
            --tests 'dev.zacsweers.metro.compiler.*.DiagnosticTestGenerated$Rich.testMissingBindingRich' \
            ${{ github.ref != 'refs/heads/main' && '-Dorg.gradle.configuration-cache.read-only=true' || '' }}

      - name: Test shaded compiler through Gradle
        run: |
          ./scripts/run-ci-gradle.sh :gradle-plugin:functionalTest --quiet \
            $DEVKIT_NARROW \
            -Pmetro.testJavaVersion=21 \
            "-Dorg.gradle.java.installations.paths=$METRO_TEST_JAVA_HOME" \
            --tests 'dev.zacsweers.metro.gradle.MetroArtifactsTest.shaded compiler renders rich diagnostics' \
            --tests 'dev.zacsweers.metro.gradle.MetroArtifactsTest.diagnosticsRenderMode is not a compilation input' \
            ${{ github.ref != 'refs/heads/main' && '-Dorg.gradle.configuration-cache.read-only=true' || '' }}

      - name: (Fail-only) Upload reports
        if: failure()
        uses: actions/upload-artifact@v7
        with:
          name: reports-shaded-compiler-smoke
          path: |
            **/build/reports/**

      - name: Publish Test Report
        uses: mikepenz/action-junit-report@v6
        if: failure()
        with:
          report_paths: '**/build/test-results/**/TEST-*.xml'
          check_name: 'Test Report - shaded compiler / Ubuntu JDK 21'

  js-box:
    name: ${{ github.ref == 'refs/heads/main' && 'js box / full' || 'web / smoke' }}
    runs-on: ubuntu-latest
    timeout-minutes: 40
    permissions:
      checks: write

    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7

      - name: Setup Gradle
        uses: ./.github/actions/setup-gradle
        with:
          encryption-key: ${{ secrets.GRADLE_ENCRYPTION_KEY }}
          cache-read-only: true

      - name: Generate tests
        run: |
          ./gradlew :compiler-tests:generateTests --quiet \
            $DEVKIT_NARROW '-Porg.jetbrains.kotlin.compiler.plugin.devkit.testAgainst=' \
            ${{ github.ref != 'refs/heads/main' && '-Dorg.gradle.configuration-cache.read-only=true' || '' }}

      - name: Run JS box smoke tests
        if: github.ref != 'refs/heads/main'
        run: |
          ./gradlew :compiler-tests:defaultTest --quiet \
            $DEVKIT_NARROW \
            --tests 'dev.zacsweers.metro.compiler.*.JsBoxTestGenerated$Inject$Assisted.testBasicAssistedInject' \
            --tests 'dev.zacsweers.metro.compiler.*.JsBoxTestGenerated$Multibindings.testMapProviderLazyValues' \
            --tests 'dev.zacsweers.metro.compiler.*.JsBoxTestGenerated$Suspend.testMixedSuspendChain' \
            ${{ github.ref != 'refs/heads/main' && '-Dorg.gradle.configuration-cache.read-only=true' || '' }}

      - name: Run Wasm bindings smoke test
        if: github.ref != 'refs/heads/main'
        run: |
          ./gradlew :gradle-plugin:functionalTest --quiet \
            -Pmetro.functionalTestKmpTarget=wasmJs \
            $DEVKIT_NARROW \
            --tests 'dev.zacsweers.metro.gradle.incremental.ICTests.icWorksWhenAddingAParamToExistingInjectedTypeWithScopeWithZeroToOneParams*' \
            -Dorg.gradle.configuration-cache.read-only=true

      - name: Run JS box tests
        if: github.ref == 'refs/heads/main'
        run: |
          ./gradlew :compiler-tests:defaultTest --continue --quiet \
            $DEVKIT_NARROW \
            --tests 'dev.zacsweers.metro.compiler.*.JsBoxTestGenerated*' \
            --tests 'dev.zacsweers.metro.compiler.*.JsFastInitBoxTestGenerated*' \
            --tests 'dev.zacsweers.metro.compiler.*.JsContributionProvidersBoxTestGenerated*'

      - name: (Fail-only) Upload reports
        if: failure()
        uses: actions/upload-artifact@v7
        with:
          name: reports-js-box
          path: |
            **/build/reports/**

      - name: Publish Test Report
        uses: mikepenz/action-junit-report@v6
        if: failure()
        with:
          report_paths: '**/build/test-results/**/TEST-*.xml'
          check_name: 'Test Report - js box'

  # Main-only: run each non-JVM target of :gradle-plugin:functionalTest in its own runner so
  # the slow IC test classes (BindingContainerICTests, ICTests, GenerateContributionProvidersICTests)
  # parallelize across targets instead of serializing inside one job. The `compatibility` job keeps
  # these parameterized tests JVM-only on PR/branch runs.
  kmp-functional:
    name: ${{ format('kmp functional / {0}{1}', matrix.target, github.ref != 'refs/heads/main' && ' (skipped)' || '') }}
    runs-on: ubuntu-latest
    permissions:
      checks: write
    strategy:
      fail-fast: false
      matrix:
        target: [js, wasmJs, linuxX64]

    steps:
      - name: Skip on PRs
        if: github.ref != 'refs/heads/main'
        run: echo "Skipping kmp functional tests on PRs to reduce CI time"

      - name: Checkout
        if: github.ref == 'refs/heads/main'
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7

      - name: Setup Gradle
        if: github.ref == 'refs/heads/main'
        uses: ./.github/actions/setup-gradle
        with:
          encryption-key: ${{ secrets.GRADLE_ENCRYPTION_KEY }}
          cache-read-only: true
          # These jobs restore the versioned Konan cache written by the default Kotlin core lane.
          konan: true

      - name: ':gradle-plugin:functionalTest'
        if: github.ref == 'refs/heads/main'
        run: |
          ./gradlew :gradle-plugin:functionalTest --continue --quiet \
            $DEVKIT_NARROW '-Porg.jetbrains.kotlin.compiler.plugin.devkit.testAgainst=' \
            -Pmetro.functionalTestKmpTarget=${{ matrix.target }}

      - name: (Fail-only) Upload reports
        if: failure() && github.ref == 'refs/heads/main'
        uses: actions/upload-artifact@v7
        with:
          name: reports-kmp-functional-${{ matrix.target }}
          path: |
            **/build/reports/**

      - name: Publish Test Report
        uses: mikepenz/action-junit-report@v6
        if: failure() && github.ref == 'refs/heads/main'
        with:
          report_paths: '**/build/test-results/**/TEST-*.xml'
          check_name: 'Test Report - kmp functional / ${{ matrix.target }}'

  samples:
    name: samples
    runs-on: ubuntu-latest
    permissions:
      checks: write

    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7

      - name: Setup Gradle
        uses: ./.github/actions/setup-gradle
        with:
          encryption-key: ${{ secrets.GRADLE_ENCRYPTION_KEY }}
          cache-read-only: true
          konan: true

      - name: Check samples
        run: |
          # We have to regen package locks because the kotlin yarn/node/etc build tooling is brittle
          ./gradlew -p samples kotlinWasmUpgradePackageLock kotlinUpgradePackageLock --rerun-tasks --no-build-cache --no-configuration-cache --quiet ${{ github.ref != 'refs/heads/main' && '-Dorg.gradle.configuration-cache.read-only=true' || '' }}
          ./gradlew -p samples check --continue --quiet ${{ github.ref != 'refs/heads/main' && '-Dorg.gradle.configuration-cache.read-only=true' || '' }}

      - name: (Fail-only) Upload reports
        if: failure()
        uses: actions/upload-artifact@v7
        with:
          name: reports-samples
          path: |
            **/build/reports/**

      - name: Publish Test Report
        uses: mikepenz/action-junit-report@v6
        if: failure()
        with:
          report_paths: 'samples/**/build/test-results/**/TEST-*.xml'
          check_name: 'Test Report - samples'

  idea-plugin:
    name: idea-plugin
    runs-on: ubuntu-latest
    permissions:
      checks: write

    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7

      - name: Setup Gradle
        uses: ./.github/actions/setup-gradle
        with:
          encryption-key: ${{ secrets.GRADLE_ENCRYPTION_KEY }}
          cache-read-only: true

      - name: Test idea-plugin
        run: ./gradlew -p idea-plugin test --quiet ${{ github.ref != 'refs/heads/main' && '-Dorg.gradle.configuration-cache.read-only=true' || '' }}

      - name: (Fail-only) Upload reports
        if: failure()
        uses: actions/upload-artifact@v7
        with:
          name: reports-idea-plugin
          path: |
            **/build/reports/**

      - name: Publish Test Report
        uses: mikepenz/action-junit-report@v6
        if: failure()
        with:
          report_paths: 'idea-plugin/**/build/test-results/**/TEST-*.xml'
          check_name: 'Test Report - idea-plugin'

  # Run multiplatform tests on main only to reduce PR time
  # Note: We use step-level conditions instead of job-level to ensure matrix expands and shows nice names
  multiplatform-tests:
    name: ${{ format('multiplatform / {0}{1}', matrix.os, github.ref != 'refs/heads/main' && ' (skipped)' || '') }}
    runs-on: ${{ github.ref == 'refs/heads/main' && matrix.os || 'ubuntu-latest' }}
    permissions:
      checks: write
    # Windows runners default to PowerShell, which mis-parses gradle `-Pkey.with.dots=value` args
    # (splits on the dot). Forcing bash everywhere keeps `./gradlew … -P…` consistent across OSes;
    # Git Bash ships with the Windows runner image.
    defaults:
      run:
        shell: bash

    strategy:
      fail-fast: false
      matrix:
        os: [ windows-latest, macos-latest ]

    steps:
      - name: Skip on PRs
        if: github.ref != 'refs/heads/main'
        run: echo "Skipping multiplatform tests on PRs to reduce CI time"

      - name: Checkout
        if: github.ref == 'refs/heads/main'
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7

      # https://github.com/gradle/actions/issues/1013
      - name: Setup Gradle
        if: github.ref == 'refs/heads/main'
        uses: ./.github/actions/setup-gradle
        with:
          encryption-key: ${{ secrets.GRADLE_ENCRYPTION_KEY }}
          # The core job writes Linux caches; these lanes only write their host-specific caches.
          cache-read-only: ${{ github.ref != 'refs/heads/main' || matrix.os == 'ubuntu-latest' }}
          konan: true
          konan-cache-read-only: ${{ github.ref != 'refs/heads/main' || matrix.os == 'ubuntu-latest' }}

      - name: Check runtime and IC tests
        if: github.ref == 'refs/heads/main'
        # `all` exercises every KmpTarget entry on this OS — host native is OS-specific (macosArm64,
        # mingwX64, etc.), so each runner covers a different native target naturally.
        run: |
          ./gradlew :runtime:allTests :runtime-coroutines:allTests :gradle-plugin:functionalTest --continue --quiet \
            $DEVKIT_NARROW '-Porg.jetbrains.kotlin.compiler.plugin.devkit.testAgainst=' \
            -Pmetro.functionalTestKmpTarget=all

      - name: Check samples
        if: github.ref == 'refs/heads/main'
        run: ./gradlew -p samples test allTests --continue --quiet

      - name: (Fail-only) Upload reports
        if: failure() && github.ref == 'refs/heads/main'
        uses: actions/upload-artifact@v7
        with:
          name: reports-multiplatform-${{ matrix.os }}
          path: |
            **/build/reports/**

      - name: Publish Test Report
        uses: mikepenz/action-junit-report@v6
        if: failure() && github.ref == 'refs/heads/main'
        with:
          report_paths: |
            **/build/test-results/**/TEST-*.xml
            samples/**/build/test-results/**/TEST-*.xml
          check_name: 'Test Report - multiplatform / ${{ matrix.os }}'

  benchmarks:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7

      - name: Setup Gradle
        uses: ./.github/actions/setup-gradle
        with:
          encryption-key: ${{ secrets.GRADLE_ENCRYPTION_KEY }}
          cache-read-only: true
          konan: true

      - name: Build Metro benchmark
        run: |
          cd benchmark
          ./run_benchmarks.sh metro --build-only

      - name: (Fail-only) Upload reports
        if: failure()
        uses: actions/upload-artifact@v7
        with:
          name: reports-benchmarks
          path: |
            **/build/reports/**

  # https://jakewharton.com/fan-in-to-a-single-required-github-action/
  final-status:
    if: always()
    runs-on: ubuntu-slim
    needs:
      - format
      - generate-matrix
      - core
      - compatibility
      - shaded-compiler-smoke
      - js-box
      - kmp-functional
      - benchmarks
      - idea-plugin
      - samples
    steps:
      - name: Check
        run: |
          results=$(tr -d '\n' <<< '${{ toJSON(needs.*.result) }}')
          if ! grep -q -v -E '(failure|cancelled)' <<< "$results"; then
            echo "One or more required jobs failed"
            exit 1
          fi

  publish:
    name: Publish snapshot (main only)
    runs-on: macos-latest
    if: github.ref == 'refs/heads/main' && github.repository == 'zacsweers/metro'
    needs:
      - final-status

    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7

      - name: Setup Gradle
        uses: ./.github/actions/setup-gradle
        with:
          encryption-key: ${{ secrets.GRADLE_ENCRYPTION_KEY }}
          cache-read-only: true
          konan: true

      - name: Upload snapshot
        # Sonatype keeps giving 429 errors lately, so no need to mark this as failure for now
        continue-on-error: true
        env:
          ORG_GRADLE_PROJECT_mavenCentralUsername: ${{ secrets.SonatypeUsername }}
          ORG_GRADLE_PROJECT_mavenCentralPassword: ${{ secrets.SonatypePassword }}
        run: ./metrow publish
